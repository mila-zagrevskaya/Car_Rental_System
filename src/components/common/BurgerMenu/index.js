import React from "react";

const BurgerIcon = () => {
  return (
    <div className="burger">
      <button className="burger-icon" type="button"></button>
    </div>
  );
};

export default BurgerIcon;
